export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: 'Admin UI',
  description: 'Admin UI for Takeaway',
  template: '%s - Admin UI',
  favicon: '/favicon.ico',
  company: 'Prasoni AI',
};
